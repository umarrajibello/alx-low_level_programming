#include <string.h>

/**
 * _strlen - prints the length of string
 * @s: the string in question received from the user
 *
 * Return: returns the int value of length of s.
 */
int _strlen(char *s)
{
	return (strlen(s));
}
