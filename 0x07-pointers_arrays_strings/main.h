#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <string.h>

void _putchar(char);
char *_memset(char *, char, unsigned int);
char *_memcpy(char *, char *, unsigned int);
char *_strchr(char *, char);

#endif /* MAIN_H */
