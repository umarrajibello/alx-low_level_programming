/**
 * mul - program to check wether a lowercase char is entered
 * @a: is 1st number received from user
 * @b: is 2nd number received from user
 *
 * Return: the product of a and b.
 */
int mul(int a, int b)
{
	return (a * b);
}
